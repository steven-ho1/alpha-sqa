# On utilise pip-tools pour generer le requirements.txt afin d'obtenir les dernières versions compatibles
# 1) pip install pip-tools
# 2) pip-compile (an argument can be provided to specify .in file. Default is requirements.in)

boto3
ruff
python-dotenv